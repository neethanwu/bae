/**
 * bae init — Guided onboarding wizard.
 * Placeholder — full implementation in Task 4.
 */
export async function runInit(): Promise<void> {
	console.log("bae init is not yet implemented. Set env vars manually:");
	console.log("  TELEGRAM_BOT_TOKEN=your-token");
	console.log("  BAE_ALLOWED_USERS=your-telegram-user-id");
	console.log("  BAE_CWD=~/baesment");
	console.log("\nWrite them to ~/.bae/.env, then run `bae start`.");
}
